# Legacy pressure-only monitor

This is the pressure-only Arduino firmware that was current immediately before
the SHT40 was added to the compressed-air monitor. It is retained for reference
only. Current firmware lives under `firmware/examples/pressure_monitor/` and
publishes pressure, temperature, and humidity.
