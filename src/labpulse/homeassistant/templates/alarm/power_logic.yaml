# Direct Geekworm X1200 GPIO6 power lifecycle. GPIO state is confirmed with
# short timers; battery voltage and percentage are telemetry only.

input_numbers:
  - id: "labpulse_[[ service.service_id ]]_power_last_outage_duration"
    config:
      name: "[[ service.label ]] Last Outage Duration"
      min: 0
      max: 604800
      step: 1
      unit_of_measurement: s
      mode: box

input_selects:
  - id: "labpulse_[[ service.service_id ]]_power_state"
    config:
      name: "[[ service.label ]] Power State"
      options:
        - Normal
        - On Battery
        - Sensor Fault

input_booleans:
  - id: "labpulse_[[ service.service_id ]]_power_muted"
    config:
      name: "[[ service.label ]] Power Alerts Muted"
  - id: "labpulse_[[ service.service_id ]]_power_outage_active"
    config:
      name: "[[ service.label ]] Outage Active"
  - id: "labpulse_[[ service.service_id ]]_power_sensor_fault_confirmed"
    config:
      name: "[[ service.label ]] Confirmed GPIO Fault"

input_datetimes:
  - id: "labpulse_[[ service.service_id ]]_power_outage_started"
    config: {name: "[[ service.label ]] Outage Started", has_date: true, has_time: true}
  - id: "labpulse_[[ service.service_id ]]_power_last_outage_started"
    config: {name: "[[ service.label ]] Last Outage Started", has_date: true, has_time: true}

sensors: []

templates:
  - sensor:
      - name: "[[ power.unique_ids.last_outage_started ]]"
        unique_id: "[[ power.unique_ids.last_outage_started ]]"
        icon: mdi:calendar-clock
        state: >
          {% set value = states('[[ power.entities.last_outage_started ]]') %}
          {{ 'Unknown' if value in ['unknown', 'unavailable', 'none', 'None', ''] else value }}
      - name: "[[ power.unique_ids.last_outage_duration ]]"
        unique_id: "[[ power.unique_ids.last_outage_duration ]]"
        icon: mdi:timer-outline
        device_class: duration
        unit_of_measurement: s
        state_class: measurement
        state: "{{ states('[[ power.entities.last_outage_duration ]]') | float(0) | round(0) }}"
    binary_sensor:
      - name: "[[ power.unique_ids.mains_present ]]"
        unique_id: "[[ power.unique_ids.mains_present ]]"
        device_class: power
        availability: "{{ is_number(states('[[ power.mains_present.mqtt_entity.entity_id ]]')) }}"
        state: "{{ states('[[ power.mains_present.mqtt_entity.entity_id ]]') | float(0) >= 0.5 }}"
        attributes:
          source: "[[ power.config.source ]]"
          gpio_chip: "[[ power.config.gpio_chip ]]"
          gpio_line: "[[ power.config.gpio_line ]]"
          configured_active_high: "[[ power.config.mains_present_active_high ]]"
          meaning: "On means external power present; off means running on UPS battery"
      - name: "[[ power.unique_ids.sensor_fault ]]"
        unique_id: "[[ power.unique_ids.sensor_fault ]]"
        device_class: problem
        state: >
          {% set raw = states('[[ power.mains_present.mqtt_entity.entity_id ]]') %}
          {% set status = states('[[ service.status_entity.entity_id ]]') %}
          {% set service_blocked = is_state('[[ service.entities.health_unhealthy ]]', 'on')
             or is_state('[[ service.entities.health_fault_active ]]', 'on') %}
          {% if service_blocked %}
            {{ is_state('[[ power.entities.sensor_fault_confirmed ]]', 'on') }}
          {% else %}
            {{ not is_number(raw) or status == 'gpio_fault' }}
          {% endif %}
        attributes:
          raw_mains_state: "{{ states('[[ power.mains_present.mqtt_entity.entity_id ]]') }}"
          service_status: "{{ states('[[ service.status_entity.entity_id ]]') }}"
          maximum_measurement_age_seconds: "[[ power.maximum_measurement_age_seconds ]]"

automations:
  - id: "labpulse_[[ service.service_id ]]_power_outage_confirm"
    alias: "LabPulse [[ service.label ]] Outage Confirm"
    mode: single
    trigger:
      - platform: state
        entity_id: "[[ power.entities.mains_present ]]"
        to: "off"
        for: {seconds: "[[ power.config.outage_confirm_seconds ]]"}
    condition:
      - condition: state
        entity_id: "[[ power.entities.outage_active ]]"
        state: "off"
      - condition: state
        entity_id: "[[ power.entities.sensor_fault ]]"
        state: "off"
    action:
      - variables:
          outage_start: "{{ as_timestamp(trigger.to_state.last_changed, as_timestamp(now())) }}"
      - service: input_datetime.set_datetime
        target: {entity_id: "[[ power.entities.outage_started ]]"}
        data: {timestamp: "{{ outage_start }}"}
      - service: input_boolean.turn_on
        target: {entity_id: "[[ power.entities.outage_active ]]"}
      - service: input_select.select_option
        target: {entity_id: "[[ power.entities.state ]]"}
        data: {option: "On Battery"}
      - choose:
          - conditions:
              - condition: state
                entity_id: "[[ power.entities.muted ]]"
                state: "off"
              - condition: state
                entity_id: "[[ model.entities.global_muted ]]"
                state: "off"
            sequence:
              - service: persistent_notification.create
                data:
                  title: >
                    {{ ('[TEST] ' if is_state('[[ model.entities.test_mode ]]', 'on') else '') ~ 'LabPulse external power lost' }}
                  message: >
                    [[ service.label ]] has directly detected loss of external power on GPIO[[ power.config.gpio_line ]]. The monitor is running on UPS battery. Battery level: {{ states('[[ power.battery_level.mqtt_entity.entity_id ]]') }}%. Battery voltage: {{ states('[[ power.voltage.mqtt_entity.entity_id ]]') }} V.
                    [[ power.mains_present.notification_context ]]
                  notification_id: "labpulse_[[ service.service_id ]]_power"
              - service: mqtt.publish
                data:
                  topic: "[[ model.sms_send_topic ]]"
                  qos: 1
                  retain: false
                  payload: >
                    {{ {"request_id": "labpulse_[[ service.service_id ]]_power_warning_" ~ now().strftime('%Y%m%dT%H%M%S%f'), "event": "warning", "service": "[[ service.name ]]", "service_label": "[[ service.label ]]", "measurement": "power", "measurement_label": "External power", "state": "On Battery", "title": [[ sms.alerts.power_warning.title ]], "message": ([[ sms.alerts.power_warning.message ]]) ~ "\n[[ power.mains_present.notification_context ]]", "test_mode": is_state('[[ model.entities.test_mode ]]', 'on'), "current_measurement": states('[[ power.voltage.mqtt_entity.entity_id ]]')} | to_json }}

  - id: "labpulse_[[ service.service_id ]]_power_recovery_confirm"
    alias: "LabPulse [[ service.label ]] Recovery Confirm"
    mode: single
    trigger:
      - platform: state
        entity_id: "[[ power.entities.mains_present ]]"
        to: "on"
        for: {seconds: "[[ power.config.restore_confirm_seconds ]]"}
    condition:
      - condition: state
        entity_id: "[[ power.entities.outage_active ]]"
        state: "on"
      - condition: state
        entity_id: "[[ power.entities.sensor_fault ]]"
        state: "off"
    action:
      - variables:
          outage_start: "{{ as_timestamp(states('[[ power.entities.outage_started ]]'), as_timestamp(now())) }}"
          recovery_start: "{{ as_timestamp(trigger.to_state.last_changed, as_timestamp(now())) }}"
          outage_duration: "{{ [0, recovery_start - outage_start] | max | round(0) }}"
      - service: input_datetime.set_datetime
        target: {entity_id: "[[ power.entities.last_outage_started ]]"}
        data: {timestamp: "{{ outage_start }}"}
      - service: input_number.set_value
        target: {entity_id: "[[ power.entities.last_outage_duration ]]"}
        data: {value: "{{ outage_duration }}"}
      - service: input_boolean.turn_off
        target: {entity_id: "[[ power.entities.outage_active ]]"}
      - service: input_select.select_option
        target: {entity_id: "[[ power.entities.state ]]"}
        data: {option: "Normal"}
      - choose:
          - conditions:
              - condition: state
                entity_id: "[[ power.entities.muted ]]"
                state: "off"
              - condition: state
                entity_id: "[[ model.entities.global_muted ]]"
                state: "off"
            sequence:
              - service: persistent_notification.create
                data:
                  title: >
                    {{ ('[TEST] ' if is_state('[[ model.entities.test_mode ]]', 'on') else '') ~ 'LabPulse external power restored' }}
                  message: >
                    [[ service.label ]] has directly detected restored external power after {{ outage_duration }} seconds. Battery level: {{ states('[[ power.battery_level.mqtt_entity.entity_id ]]') }}%. Battery voltage: {{ states('[[ power.voltage.mqtt_entity.entity_id ]]') }} V.
                    [[ power.mains_present.notification_context ]]
                  notification_id: "labpulse_[[ service.service_id ]]_power"
              - service: mqtt.publish
                data:
                  topic: "[[ model.sms_send_topic ]]"
                  qos: 1
                  retain: false
                  payload: >
                    {{ {"request_id": "labpulse_[[ service.service_id ]]_power_recovery_" ~ now().strftime('%Y%m%dT%H%M%S%f'), "event": "recovery", "service": "[[ service.name ]]", "service_label": "[[ service.label ]]", "measurement": "power", "measurement_label": "External power", "state": "Normal", "title": [[ sms.alerts.power_recovery.title ]], "message": ([[ sms.alerts.power_recovery.message ]]) ~ "\n[[ power.mains_present.notification_context ]]", "test_mode": is_state('[[ model.entities.test_mode ]]', 'on'), "current_measurement": states('[[ power.voltage.mqtt_entity.entity_id ]]')} | to_json }}

  - id: "labpulse_[[ service.service_id ]]_power_sensor_fault"
    alias: "LabPulse [[ service.label ]] Power Sensor Fault"
    mode: restart
    trigger:
      - platform: state
        entity_id: "[[ power.entities.sensor_fault ]]"
        to: "on"
      - platform: homeassistant
        event: start
      - platform: event
        event_type: automation_reloaded
    action:
      - delay: {seconds: 2}
      - condition: state
        entity_id: "[[ power.entities.sensor_fault ]]"
        state: "on"
      - condition: state
        entity_id: "[[ power.entities.sensor_fault_confirmed ]]"
        state: "off"
      - service: input_boolean.turn_on
        target: {entity_id: "[[ power.entities.sensor_fault_confirmed ]]"}
      - service: input_select.select_option
        target: {entity_id: "[[ power.entities.state ]]"}
        data: {option: "Sensor Fault"}
      - choose:
          - conditions:
              - condition: state
                entity_id: "[[ power.entities.muted ]]"
                state: "off"
              - condition: state
                entity_id: "[[ model.entities.global_muted ]]"
                state: "off"
            sequence:
              - service: persistent_notification.create
                data:
                  title: >
                    {{ ('[TEST] ' if is_state('[[ model.entities.test_mode ]]', 'on') else '') ~ 'LabPulse external-power sensor fault' }}
                  message: >
                    [[ service.label ]] cannot read the X1200 external-power signal. Raw mains state: {{ states('[[ power.mains_present.mqtt_entity.entity_id ]]') }}. Service status: {{ states('[[ service.status_entity.entity_id ]]') }}. This is not being treated as an outage.
                    [[ power.mains_present.notification_context ]]
                  notification_id: "labpulse_[[ service.service_id ]]_power"
              - service: mqtt.publish
                data:
                  topic: "[[ model.sms_send_topic ]]"
                  qos: 1
                  retain: false
                  payload: >
                    {{ {"request_id": "labpulse_[[ service.service_id ]]_power_sensor_fault_" ~ now().strftime('%Y%m%dT%H%M%S%f'), "event": "sensor_fault", "service": "[[ service.name ]]", "service_label": "[[ service.label ]]", "measurement": "power", "measurement_label": "External-power GPIO", "state": "Sensor Fault", "title": [[ sms.alerts.power_sensor_fault.title ]], "message": ([[ sms.alerts.power_sensor_fault.message ]]) ~ "\n[[ power.mains_present.notification_context ]]", "test_mode": is_state('[[ model.entities.test_mode ]]', 'on'), "current_measurement": states('[[ power.mains_present.mqtt_entity.entity_id ]]')} | to_json }}

  - id: "labpulse_[[ service.service_id ]]_power_sensor_recovery"
    alias: "LabPulse [[ service.label ]] Power Sensor Recovery"
    mode: single
    trigger:
      - platform: state
        entity_id: "[[ power.entities.sensor_fault ]]"
        from: "on"
        to: "off"
    condition:
      - condition: state
        entity_id: "[[ power.entities.sensor_fault_confirmed ]]"
        state: "on"
    action:
      - service: input_boolean.turn_off
        target: {entity_id: "[[ power.entities.sensor_fault_confirmed ]]"}
      - choose:
          - conditions: "{{ is_state('[[ power.entities.outage_active ]]', 'on') }}"
            sequence:
              - service: input_select.select_option
                target: {entity_id: "[[ power.entities.state ]]"}
                data: {option: "On Battery"}
        default:
          - service: input_select.select_option
            target: {entity_id: "[[ power.entities.state ]]"}
            data: {option: "Normal"}

  - id: "labpulse_[[ service.service_id ]]_power_reconcile"
    alias: "LabPulse [[ service.label ]] Power Reconcile"
    mode: restart
    trigger:
      - platform: homeassistant
        event: start
      - platform: event
        event_type: automation_reloaded
    action:
      - delay: {seconds: 1}
      - choose:
          - conditions: "{{ is_state('[[ power.entities.sensor_fault ]]', 'on') and is_state('[[ power.entities.sensor_fault_confirmed ]]', 'on') }}"
            sequence:
              - service: input_select.select_option
                target: {entity_id: "[[ power.entities.state ]]"}
                data: {option: "Sensor Fault"}
          - conditions: "{{ is_state('[[ power.entities.outage_active ]]', 'on') and is_state('[[ power.entities.mains_present ]]', 'off') }}"
            sequence:
              - service: input_select.select_option
                target: {entity_id: "[[ power.entities.state ]]"}
                data: {option: "On Battery"}
          - conditions: "{{ is_state('[[ power.entities.outage_active ]]', 'off') and is_state('[[ power.entities.mains_present ]]', 'on') }}"
            sequence:
              - service: input_select.select_option
                target: {entity_id: "[[ power.entities.state ]]"}
                data: {option: "Normal"}

  - id: "labpulse_[[ service.service_id ]]_power_reconcile_outage"
    alias: "LabPulse [[ service.label ]] Reconcile Missed Outage"
    mode: restart
    trigger:
      - platform: homeassistant
        event: start
      - platform: event
        event_type: automation_reloaded
    action:
      - delay: {seconds: "[[ power.config.outage_confirm_seconds ]]"}
      - condition: state
        entity_id: "[[ power.entities.mains_present ]]"
        state: "off"
      - condition: state
        entity_id: "[[ power.entities.outage_active ]]"
        state: "off"
      - condition: state
        entity_id: "[[ power.entities.sensor_fault ]]"
        state: "off"
      - service: input_datetime.set_datetime
        target: {entity_id: "[[ power.entities.outage_started ]]"}
        data: {timestamp: "{{ as_timestamp(now()) - [[ power.config.outage_confirm_seconds ]] }}"}
      - service: input_boolean.turn_on
        target: {entity_id: "[[ power.entities.outage_active ]]"}
      - service: input_select.select_option
        target: {entity_id: "[[ power.entities.state ]]"}
        data: {option: "On Battery"}
      - choose:
          - conditions:
              - condition: state
                entity_id: "[[ power.entities.muted ]]"
                state: "off"
              - condition: state
                entity_id: "[[ model.entities.global_muted ]]"
                state: "off"
            sequence:
              - service: mqtt.publish
                data:
                  topic: "[[ model.sms_send_topic ]]"
                  qos: 1
                  retain: false
                  payload: >
                    {{ {"request_id": "labpulse_[[ service.service_id ]]_power_warning_reconcile_" ~ now().strftime('%Y%m%dT%H%M%S%f'), "event": "warning", "service": "[[ service.name ]]", "service_label": "[[ service.label ]]", "measurement": "power", "measurement_label": "External power", "state": "On Battery", "title": [[ sms.alerts.power_warning.title ]], "message": ([[ sms.alerts.power_warning.message ]]) ~ "\n[[ power.mains_present.notification_context ]]", "test_mode": is_state('[[ model.entities.test_mode ]]', 'on'), "current_measurement": states('[[ power.voltage.mqtt_entity.entity_id ]]')} | to_json }}

  - id: "labpulse_[[ service.service_id ]]_power_reconcile_recovery"
    alias: "LabPulse [[ service.label ]] Reconcile Missed Recovery"
    mode: restart
    trigger:
      - platform: homeassistant
        event: start
      - platform: event
        event_type: automation_reloaded
    action:
      - delay: {seconds: "[[ power.config.restore_confirm_seconds ]]"}
      - condition: state
        entity_id: "[[ power.entities.mains_present ]]"
        state: "on"
      - condition: state
        entity_id: "[[ power.entities.outage_active ]]"
        state: "on"
      - condition: state
        entity_id: "[[ power.entities.sensor_fault ]]"
        state: "off"
      - variables:
          outage_start: "{{ as_timestamp(states('[[ power.entities.outage_started ]]'), as_timestamp(now())) }}"
          outage_duration: "{{ [0, as_timestamp(now()) - outage_start] | max | round(0) }}"
      - service: input_datetime.set_datetime
        target: {entity_id: "[[ power.entities.last_outage_started ]]"}
        data: {timestamp: "{{ outage_start }}"}
      - service: input_number.set_value
        target: {entity_id: "[[ power.entities.last_outage_duration ]]"}
        data: {value: "{{ outage_duration }}"}
      - service: input_boolean.turn_off
        target: {entity_id: "[[ power.entities.outage_active ]]"}
      - service: input_select.select_option
        target: {entity_id: "[[ power.entities.state ]]"}
        data: {option: "Normal"}
      - choose:
          - conditions:
              - condition: state
                entity_id: "[[ power.entities.muted ]]"
                state: "off"
              - condition: state
                entity_id: "[[ model.entities.global_muted ]]"
                state: "off"
            sequence:
              - service: mqtt.publish
                data:
                  topic: "[[ model.sms_send_topic ]]"
                  qos: 1
                  retain: false
                  payload: >
                    {{ {"request_id": "labpulse_[[ service.service_id ]]_power_recovery_reconcile_" ~ now().strftime('%Y%m%dT%H%M%S%f'), "event": "recovery", "service": "[[ service.name ]]", "service_label": "[[ service.label ]]", "measurement": "power", "measurement_label": "External power", "state": "Normal", "title": [[ sms.alerts.power_recovery.title ]], "message": ([[ sms.alerts.power_recovery.message ]]) ~ "\n[[ power.mains_present.notification_context ]]", "test_mode": is_state('[[ model.entities.test_mode ]]', 'on'), "current_measurement": states('[[ power.voltage.mqtt_entity.entity_id ]]')} | to_json }}
